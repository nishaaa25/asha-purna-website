// hooks/useLenis.js
'use client';

// This hook is now deprecated in favor of LenisProvider
// Kept for backwards compatibility but does nothing
export const useLenis = () => {
  // No-op - LenisProvider handles all smooth scrolling
  return null;
};